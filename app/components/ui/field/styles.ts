import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
 field: {
   backgroundColor: 'gray',
   borderRadius: 5,
   marginTop: 5,
   padding: 3,
   paddingLeft: 10,
   width: "100%",
 },
});