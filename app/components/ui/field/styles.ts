import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  field: {
    backgroundColor: "gray",
    borderRadius: 20,
    marginTop: 15,
    padding: 3,
    paddingVertical: 12,
    paddingLeft: 15,
    width: "100%",
    fontSize: 18
  }
});