import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonContainer: {
    alignItems: "center",
    justifyContent: "center",
    paddingVertical: 12,
    borderRadius: 20,
    marginTop: 15,
  },
  buttonText: {
    textAlign: "center",
    color: '#333333',
    fontSize: 18,
    fontWeight: "bold",
  },
});