import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonContainer: {
    alignItems: "center",
    justifyContent: "center",
    paddingVertical: 5,
    borderRadius: 5,
    marginTop: 5,
  },
  buttonText: {
    textAlign: "center",
    color: 'gray',
    fontSize: 16,
  },
});